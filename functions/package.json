{
  "name": "ingredeint-functions",
  "private": true,
  "engines": { "node": "^18" },
  "scripts": {
    "build": "tsc",
    "clean": "rimraf lib",
    "deploy": "firebase deploy --only functions",
    "lint": "echo noop"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "firebase-admin": "^12.5.0",
    "firebase-functions": "^4.4.0",
    "nodemailer": "^6.9.8"
  },
  "devDependencies": {
    "rimraf": "^5.0.5",
    "typescript": "^5.6.3"
  }
}
